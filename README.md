# Email-Asistant
Email Secretary Bot — A Python desktop application that automatically fetches unread emails from Gmail, stores them in a SQL Server database, and uses the Claude AI API to generate short summaries and categories for each email. Built with Python, Tkinter, pyodbc, and the Anthropic API.
